import 'package:flutter/material.dart';

class CosturmCheckbox extends StatelessWidget {
  const CosturmCheckbox({super.key});

  @override
  Widget build(BuildContext context) {
    return Checkbox(value: true, onChanged: null);
  }
}